version: '3.9'
services:
  api:
    build: .
    ports: ["8000:8000"]
  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_DB=sales
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
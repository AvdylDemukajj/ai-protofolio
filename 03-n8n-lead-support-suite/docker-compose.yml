version: '3.8'

services:
  n8n:
    image: n8nio/n8n:1.76.1
    container_name: n8n-lead-support
    restart: unless-stopped
    ports:
      - "5679:5678"
    environment:
      - N8N_HOST=0.0.0.0
      - N8N_PORT=5678
      - N8N_PROTOCOL=${N8N_PROTOCOL:-http}
      - WEBHOOK_URL=${WEBHOOK_URL:-http://localhost:5679/}
      - N8N_EDITOR_BASE_URL=${N8N_EDITOR_BASE_URL:-http://localhost:5679}
      - N8N_ENCRYPTION_KEY=${N8N_ENCRYPTION_KEY}
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=${N8N_BASIC_AUTH_USER}
      - N8N_BASIC_AUTH_PASSWORD=${N8N_BASIC_AUTH_PASSWORD}
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=db
      - DB_POSTGRESDB_PORT=5432
      - DB_POSTGRESDB_DATABASE=leads_db
      - DB_POSTGRESDB_USER=leads_user
      - DB_POSTGRESDB_PASSWORD=${DB_PASSWORD}
      - N8N_BLOCK_FILE_ACCESS_TO_N8N_FILES=true
      - EXECUTIONS_DATA_PRUNE=true
      - EXECUTIONS_DATA_MAX_AGE=168
      - WEBHOOK_SECRET=${WEBHOOK_SECRET}
      - SLACK_WEBHOOK_URL=${SLACK_WEBHOOK_URL}
    volumes:
      - n8n_data:/home/node/.n8n
      - ./workflows:/home/node/imported_workflows:ro
      - ./demo-data:/home/node/demo-data:ro
    depends_on:
      db:
        condition: service_healthy
    networks:
      - leads-network

  db:
    image: postgres:15-alpine
    container_name: n8n-leads-db
    restart: unless-stopped
    environment:
      - POSTGRES_USER=leads_user
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=leads_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/01-init.sql
      - ./database/seed_data.sql:/docker-entrypoint-initdb.d/02-seed.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U leads_user -d leads_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    ports:
      - "5433:5432"
    networks:
      - leads-network

  nginx:
    profiles:
      - production
    image: nginx:1.27-alpine
    container_name: n8n-leads-nginx
    restart: unless-stopped
    ports:
      - "8081:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - n8n
    networks:
      - leads-network

volumes:
  n8n_data:
  postgres_data:

networks:
  leads-network:
    driver: bridge
